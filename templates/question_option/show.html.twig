{% extends 'base.html.twig' %}

{% block title %}QuestionOption{% endblock %}

{% block body %}
    <h1>QuestionOption</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_option.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ question_option.Value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('question_option_index') }}">back to list</a>

    <a href="{{ path('question_option_edit', {'id': question_option.id}) }}">edit</a>

    {{ include('question_option/_delete_form.html.twig') }}
{% endblock %}
