<form method="post" action="{{ path('question_type_delete', {'id': question_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_type.id) }}">
    <button class="btn">Delete</button>
</form>
