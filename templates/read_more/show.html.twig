{% extends 'base.html.twig' %}

{% block title %}ReadMore{% endblock %}

{% block body %}
    <h1>ReadMore</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ read_more.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ read_more.content }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ read_more.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('read_more_index') }}">back to list</a>

    <a href="{{ path('read_more_edit', {'id': read_more.id}) }}">edit</a>

    {{ include('read_more/_delete_form.html.twig') }}
{% endblock %}
