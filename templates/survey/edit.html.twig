{% extends 'base.html.twig' %}

{% block title %}Edit Survey{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit Survey</h1>

        {% form_theme form 'collectionTheme.collection.html.twig' %}
        {{ include('survey/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('survey_index') }}">back to list</a>

        {{ include('survey/_delete_form.html.twig') }}
    </div>
{% endblock %}
